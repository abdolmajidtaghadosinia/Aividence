# راهنمای اضافه کردن لوگو و فاو آیکون

## 📁 ساختار پوشه‌ها

```
front/
├── assets/
│   ├── images/
│   │   └── logo.png          # لوگوی اصلی (PNG, SVG, JPG, WEBP)
│   └── icons/                # در صورت نیاز به استفاده از فایل آیکن جداگانه (به صورت پیش‌فرض خالی)
```

## 🎨 فرمت‌های پشتیبانی شده

### لوگو (Logo)
- **PNG**: بهترین کیفیت، پشتیبانی از شفافیت
- **SVG**: وکتور، مقیاس‌پذیر، حجم کم
- **JPG**: حجم کم، بدون شفافیت
- **WEBP**: کیفیت بالا، حجم کم

### فاو آیکون (Favicon)
- **SVG**: (پیش‌فرض) به صورت داده‌ی درون‌خطی در `index.html` ست شده است و نیازی به فایل مجزا ندارد.
- **ICO / PNG**: در صورت تمایل می‌توانید نسخه‌های فایل‌محور را جایگزین کنید (پوشه `icons` در گیت نادیده گرفته می‌شود تا به خاطر فایل‌های باینری تداخل ایجاد نشود).

## 📏 اندازه‌های پیشنهادی

### لوگو
- **کوچک (Header)**: 200x60 پیکسل
- **متوسط (Sidebar)**: 300x90 پیکسل
- **بزرگ (Login)**: 400x120 پیکسل

### فاو آیکون
- **درون‌خطی (Inline SVG)**: نیازی به اندازه مشخص ندارد و در تمام اندازه‌ها شفاف است.
- **اگر فایل استفاده می‌کنید**: 16x16 و 32x32 برای دسکتاپ و 180x180 برای اپل پیشنهاد می‌شود.

## 🔧 نحوه استفاده

### 1. جایگزینی فایل‌ها
فایل‌های خود را در صورت نیاز اضافه کنید (پوشه `icons` در `.gitignore` قرار دارد تا جلوی مشکلات ادغام فایل‌های باینری را بگیرد):

```bash
# کپی لوگو
cp your-logo.png front/assets/images/logo.png

# (اختیاری) اگر می‌خواهید به جای آیکن درون‌خطی از فایل استفاده کنید
cp your-favicon.ico front/assets/icons/favicon.ico
cp your-favicon-16x16.png front/assets/icons/favicon-16x16.png
cp your-favicon-32x32.png front/assets/icons/favicon-32x32.png
cp your-apple-touch-icon.png front/assets/icons/apple-touch-icon.png
```

### 2. استفاده در کامپوننت‌ها

```tsx
import Logo from '../components/Logo';

// استفاده ساده
<Logo />

// استفاده با تنظیمات
<Logo 
  size="lg"           // sm, md, lg, xl
  showText={true}     // نمایش متن
  className="custom"  // کلاس‌های اضافی
/>
```

### 3. تنظیمات Vite
فایل‌های assets به صورت خودکار توسط Vite پردازش می‌شوند و در مسیر `/assets/` در دسترس قرار می‌گیرند.

## 🎯 نکات مهم

1. **نام فایل‌ها**: نام فایل‌ها باید دقیقاً مطابق با فایل‌های موجود باشد
2. **مسیرها**: مسیرها در کد به صورت `/assets/` شروع می‌شوند
3. **Fallback**: اگر لوگو لود نشود، یک لوگوی پیش‌فرض نمایش داده می‌شود
4. **بهینه‌سازی**: برای تولید، فایل‌ها به صورت خودکار بهینه‌سازی می‌شوند

## 🚀 تست

پس از اضافه کردن فایل‌ها، پروژه را اجرا کنید:

```bash
npm run dev
```

لوگو و فاو آیکون باید در تمام صفحات نمایش داده شوند.
